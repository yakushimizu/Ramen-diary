<h1>編集画面</h1>

<%= form_with model: @item, url: update_item_path(@item.id), method: :patch do |f| %>

  <h4>店名</h4>
  <%= f.text_field :shop_name %>

  <h4>本文</h4>
  <%= f.text_area :address %>

  <%= f.submit '保存' %>

<% end %>